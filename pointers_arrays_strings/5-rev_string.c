#include "main.h"

/**
* rev_string - Reverses a string
* @s: input string
* Return: string in reverse
/*

void rev_string(char *s)
{
char rev = s[0];
int counter = 0;
int i;

while (s[counter] != '\0')
