/**
 * main - Entry point of the C program.
 *
 * Description: The main function serves as the entry point of the program.
 *              It calls the asm_main function written in assembly to print
 *              "Hello, World" using system calls.
 *
 * Return: Always returns 0 to indicate successful execution.
 */
int main(void)
{
asm_main();
return (0);
}
